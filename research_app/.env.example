# Environment variables for the Research & Summary Application

# --- Language Model Configuration ---
# Required: API key for your chosen LLM provider (e.g., OpenAI, Anthropic)
LLM_API_KEY="YOUR_LLM_API_KEY_HERE"

# Optional: Specify the model name (defaults to 'gpt-3.5-turbo' in config.py if not set)
# Check your provider's documentation for available model names.
# LLM_MODEL_NAME="gpt-4-turbo-preview"

# --- Search Tool Configuration ---
# Required: API key for your chosen search tool provider (e.g., Tavily, Google Search API)
SEARCH_API_KEY="YOUR_SEARCH_API_KEY_HERE"

# --- Optional Configuration ---
# Add any other environment-specific settings your application might need below
# EXAMPLE_SETTING="example_value"